/**
 * meili-orm 核心（零 Spring 依赖）：注解映射、实体元模型、序列化抽象、查询 IR、
 * Settings 投影、Operations 与回调契约。
 *
 * <p>本包及其子包为对外 API 面；SDK（{@code com.meilisearch.sdk}）类型不得出现在
 * 非 {@code internal} 包的公开签名中。</p>
 */
package io.github.lamspace.meili.core;
