<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent><groupId>io.github.lamspace</groupId><artifactId>meili-orm-it</artifactId><version>1.0-SNAPSHOT</version></parent>
    <artifactId>meili-orm-it-boot3</artifactId>
    <name>meili-orm-it-boot3</name>
    <description>Compatibility matrix, Boot 3.5.16 generation: starter wiring and Long-primary-key CRUD round-trips against a real server, plus version sentinels.</description>
    <dependencyManagement>
        <dependencies>
            <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers-bom</artifactId><version>${testcontainers.version}</version><type>pom</type><scope>import</scope></dependency>
            <!-- Written out explicitly although same generation as the parent baseline: symmetric self-evidence of the BOM-override mechanism on both matrix sides. -->
            <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-dependencies</artifactId><version>3.5.16</version><type>pom</type><scope>import</scope></dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency><groupId>io.github.lamspace</groupId><artifactId>spring-boot-starter-meili-orm</artifactId><version>${project.version}</version></dependency>
        <dependency><groupId>io.github.lamspace</groupId><artifactId>meili-orm-core</artifactId><type>test-jar</type><scope>test</scope></dependency>
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.testcontainers</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
        <!-- Matrix repository cases: pulled in via the explicit opt-in coordinate (the starter aggregate surface stays unchanged). -->
        <dependency><groupId>io.github.lamspace</groupId><artifactId>meili-orm-repository</artifactId><version>${project.version}</version><scope>test</scope></dependency>
        <!-- Service-connection bridge matrix cases: the opt-in testcontainers module is added explicitly (the starter aggregate surface stays unchanged). -->
        <dependency><groupId>io.github.lamspace</groupId><artifactId>meili-orm-testcontainers</artifactId><version>${project.version}</version><scope>test</scope></dependency>
    </dependencies>
    <build><plugins>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-failsafe-plugin</artifactId>
            <executions><execution><goals><goal>integration-test</goal><goal>verify</goal></goals></execution></executions></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-jar-plugin</artifactId>
            <executions><execution><goals><goal>test-jar</goal></goals></execution></executions></plugin>
    </plugins></build>
</project>
