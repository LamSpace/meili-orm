# Demo configuration: synchronous writes (wait-task) + startup settings sync (drift only warns, never acts).
meili:
  url: http://localhost:7700
  api-key: demoMasterKey-0123456789
  wait-task: true
  index:
    auto-init: sync-settings
    on-settings-drift: warn
