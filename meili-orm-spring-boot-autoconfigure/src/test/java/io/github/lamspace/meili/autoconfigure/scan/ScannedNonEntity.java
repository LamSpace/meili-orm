package io.github.lamspace.meili.autoconfigure.scan;

/** Fixture class in the scanned package that carries no {@code @MeiliDocument} and must be ignored. */
public final class ScannedNonEntity {

    /** Hidden so the class is not trivially instantiable-free for scanning realism. */
    private ScannedNonEntity() {
    }
}
